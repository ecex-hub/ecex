# 🚀 生产环境部署完整方案

## 📋 我为你准备了什么

我已经为你创建了一套**完整的生产环境部署方案**，包含：

### ✅ Docker 配置文件
- ✅ 后端 Dockerfile（PHP 7.4 + Nginx + Supervisor）
- ✅ 前端 Dockerfile（Vue 3 构建 + Caddy）
- ✅ Docker Compose 配置（开发和生产环境）
- ✅ Nginx、PHP、Redis 等所有配置文件

### ✅ GitHub Actions CI/CD
- ✅ 自动构建后端镜像
- ✅ 自动构建前端镜像
- ✅ 自动推送到 GitHub Container Registry

### ✅ 部署脚本
- ✅ 服务器初始化脚本
- ✅ 一键部署脚本
- ✅ 数据库初始化脚本
- ✅ 自动备份和恢复脚本
- ✅ 服务监控脚本

### ✅ 完整文档
- ✅ 快速开始指南（5 步部署）
- ✅ 详细部署文档（包含故障排查）
- ✅ 架构说明文档

## 🎯 部署架构

```
互联网 (HTTPS)
    ↓
前端服务器 (Caddy + Vue)
    ↓ (内网)
后端服务器 (Nginx + PHP + MySQL + Redis)
```

**特点**：
- ✅ 前端使用 Caddy 自动 HTTPS（Let's Encrypt）
- ✅ 前端反向代理到后端内网地址
- ✅ 所有服务 Docker 化
- ✅ GitHub Actions 自动构建镜像
- ✅ 服务器只需拉取镜像即可部署

## 📖 如何开始部署？

### 方式一：快速部署（推荐新手）

**阅读这个文档**：[QUICK-START.md](QUICK-START.md)

这是一个 5 步快速部署指南，包含所有必要的命令，复制粘贴即可完成部署。

### 方式二：详细部署（推荐运维）

**阅读这个文档**：[DEPLOYMENT.md](DEPLOYMENT.md)

这是完整的部署文档，包含：
- 详细的每一步说明
- 配置文件详解
- 安全加固建议
- 常见问题解决
- 运维命令大全

### 方式三：了解架构（推荐开发）

**阅读这个文档**：[DEPLOY-README.md](DEPLOY-README.md)

这是架构说明文档，包含：
- 完整的项目结构
- 技术栈说明
- 部署流程图
- 配置文件说明

## 🔥 核心步骤（超简化版）

### 1️⃣ 推送代码到 GitHub
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/你的用户名/你的仓库名.git
git push -u origin main
```

### 2️⃣ 初始化后端服务器
```bash
# SSH 登录后端服务器
wget https://raw.githubusercontent.com/你的用户名/你的仓库名/main/deploy/init-server.sh
bash init-server.sh

# 部署后端
mkdir -p ~/app/backend && cd ~/app/backend
# 下载部署文件并配置（详见 QUICK-START.md）
bash deploy.sh
bash init-database.sh
```

### 3️⃣ 初始化前端服务器
```bash
# SSH 登录前端服务器
wget https://raw.githubusercontent.com/你的用户名/你的仓库名/main/deploy/init-server.sh
bash init-server.sh

# 部署前端
mkdir -p ~/app/frontend && cd ~/app/frontend
# 下载部署文件并配置（详见 QUICK-START.md）
bash deploy.sh
```

### 4️⃣ 配置域名解析
在域名服务商处添加 A 记录指向前端服务器公网 IP

### 5️⃣ 访问网站
```
https://你的域名.com
```

## 📁 重要文件清单

### 需要修改的配置文件

在部署前，你需要修改以下文件中的占位符：

#### GitHub Actions 配置
- `.github/workflows/backend.yml` - 无需修改（自动使用仓库信息）
- `.github/workflows/frontend.yml` - 无需修改（自动使用仓库信息）

#### 后端部署配置
- `deploy/backend/docker-compose.prod.yml` - 修改镜像地址
- `deploy/backend/.env` - 配置数据库密码等
- `deploy/backend/init-database.sh` - 修改下载地址（可选）

#### 前端部署配置
- `deploy/frontend/docker-compose.prod.yml` - 修改镜像地址
- `deploy/frontend/Caddyfile` - 修改域名和后端地址
- `deploy/frontend/.env` - 配置后端内网地址

### 需要替换的占位符

在所有文件中，将以下占位符替换为实际值：

- `YOUR_GITHUB_USERNAME` → 你的 GitHub 用户名
- `YOUR_REPO_NAME` → 你的仓库名
- `your-domain.com` → 你的实际域名
- `your-email@example.com` → 你的邮箱
- `172.16.0.100` → 后端服务器的实际内网 IP
- `your_strong_mysql_password_here` → 强密码
- `your_strong_redis_password_here` → 强密码

## 🛠️ 日常运维

### 更新应用
```bash
# 推送代码到 GitHub，GitHub Actions 自动构建镜像
git push

# 后端服务器
cd ~/app/backend && bash deploy.sh

# 前端服务器
cd ~/app/frontend && bash deploy.sh
```

### 备份数据
```bash
cd ~/app/backend
bash backup.sh

# 设置定时备份
crontab -e
# 添加: 0 2 * * * cd ~/app/backend && bash backup.sh >> /var/log/backup.log 2>&1
```

### 查看日志
```bash
# 后端
cd ~/app/backend
docker-compose -f docker-compose.prod.yml logs -f app

# 前端
cd ~/app/frontend
docker-compose -f docker-compose.prod.yml logs -f frontend
```

### 监控服务
```bash
bash deploy/monitor.sh
```

## ⚠️ 重要提醒

### 部署前必做
1. ✅ 准备两台阿里云服务器（Alibaba Cloud Linux 3.2104 LTS）
2. ✅ 准备一个已备案的域名
3. ✅ 创建 GitHub 仓库
4. ✅ 生成 GitHub Personal Access Token（需要 `write:packages` 权限）
5. ✅ 确保两台服务器在同一 VPC 内网

### 部署后必做
1. ✅ 修改所有默认密码（数据库、Redis）
2. ✅ 配置防火墙规则
3. ✅ 设置定时备份任务
4. ✅ 测试所有功能是否正常

### 安全建议
1. ✅ 后端服务器只允许前端服务器内网访问
2. ✅ 定期更新系统和 Docker 镜像
3. ✅ 启用日志监控和告警
4. ✅ 定期检查备份是否正常

## 🎓 学习路径

### 如果你是新手
1. 先阅读 [QUICK-START.md](QUICK-START.md)
2. 按照步骤一步步操作
3. 遇到问题查看 [DEPLOYMENT.md](DEPLOYMENT.md) 的故障排查部分

### 如果你是运维
1. 先阅读 [DEPLOY-README.md](DEPLOY-README.md) 了解架构
2. 阅读 [DEPLOYMENT.md](DEPLOYMENT.md) 了解详细配置
3. 根据实际情况调整配置文件

### 如果你是开发
1. 先阅读 [DEPLOY-README.md](DEPLOY-README.md) 了解技术栈
2. 查看 Dockerfile 和配置文件了解构建过程
3. 根据需要修改 GitHub Actions 工作流

## 📞 获取帮助

### 文档索引
- 📘 [QUICK-START.md](QUICK-START.md) - 快速开始（5 步部署）
- 📗 [DEPLOYMENT.md](DEPLOYMENT.md) - 详细部署文档
- 📕 [DEPLOY-README.md](DEPLOY-README.md) - 架构说明

### 常见问题
详见 [DEPLOYMENT.md](DEPLOYMENT.md) 的"常见问题"章节

---

## ✨ 总结

这套部署方案的优势：

1. **自动化程度高**：GitHub Actions 自动构建，一键部署
2. **安全性好**：HTTPS 自动化，内网隔离，容器隔离
3. **易于维护**：Docker 化部署，一键更新，自动备份
4. **文档完善**：三份文档覆盖不同需求
5. **生产就绪**：所有配置都是生产环境优化

**现在就开始部署吧！** 👉 [QUICK-START.md](QUICK-START.md)

