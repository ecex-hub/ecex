# 开发环境配置（HTTP）
:80 {
    # 根目录
    root * /srv
    
    # 启用文件服务
    file_server
    
    # 启用Gzip压缩
    encode gzip
    
    # Vue Router的history模式支持
    try_files {path} /index.html
    
    # 静态资源缓存
    @static {
        path *.js *.css *.png *.jpg *.jpeg *.gif *.ico *.svg *.woff *.woff2 *.ttf *.eot
    }
    header @static Cache-Control "public, max-age=31536000, immutable"
    
    # 日志
    log {
        output stdout
        format console
    }
}

# 生产环境配置（HTTPS自动）
# 取消下面的注释并替换your-domain.com
# your-domain.com {
#     root * /srv
#     file_server
#     encode gzip
#     try_files {path} /index.html
#     
#     @static {
#         path *.js *.css *.png *.jpg *.jpeg *.gif *.ico *.svg *.woff *.woff2 *.ttf *.eot
#     }
#     header @static Cache-Control "public, max-age=31536000, immutable"
#     
#     log {
#         output stdout
#         format console
#     }
# }

